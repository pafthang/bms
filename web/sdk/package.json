{
  "name": "@pafthang/bms-sdk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "generate": "node -e \"const fs=require('node:fs'); fs.rmSync('src/generated',{recursive:true,force:true});\" && openapi --input ../../openapi/openapi.json --output src/generated --client fetch --exportCore true --exportServices true --exportModels true --exportSchemas false",
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf src/generated dist"
  },
  "devDependencies": {
    "openapi-typescript-codegen": "^0.29.0",
    "typescript": "^5.9.2"
  }
}
